services:
  writing-assistant:
    build: .
    container_name: writing-assistant
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "80:8010"
    volumes:
      - ./data:/app/data
